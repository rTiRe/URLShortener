{
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node .",
    "prestart": "npm run build",
    "server:watch": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "@types/express": "^5.0.3",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "log4js": "^6.9.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
